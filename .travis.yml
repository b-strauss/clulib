language: node_js
node_js:
  - '6'

cache:
  yarn: true
  directories:
    - ./node_modules

notifications:
  email: true

matrix:
  fast_finish: true

install:
  - yarn install

before_script:
  - yarn check-lint
  - yarn compile

script:
  - yarn test
