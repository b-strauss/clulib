goog.module('clulib.form.form_elements');

const {ValueAccessor} = goog.require('clulib.form.value_accessors');

class FormElement {
  /**
   * @param {Element} element
   * @param {ValueAccessor} valueAccessor
   */
// eslint-disable-next-line no-useless-constructor
  constructor (element, valueAccessor) {
  }
}
